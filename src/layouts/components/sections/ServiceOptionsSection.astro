---
import type { z } from "astro/zod";
import type { Section } from "@/types";
import Icons from "@/helpers/Icons.astro";
import { getEntryCTM } from "@/lib/contentParser.astro";
import { markdownify } from "@/lib/utils/textConverter";
import overrideObjects from "@/lib/utils/overrideObjects.ts";
import type { serviceOptionsSectionSchema } from "@/sections.schema";

type ServiceOptionsContent = Section &
  NonNullable<z.infer<typeof serviceOptionsSectionSchema>>;

type Props = {
  content?: ServiceOptionsContent;
};

const defaultContent = (
  await getEntryCTM(
    "sections",
    "hvac-installation-options-section",
    Astro.currentLocale,
  )
)?.data as ServiceOptionsContent;

const actualContent = overrideObjects(
  { ...defaultContent },
  Astro.props.content || {},
) as ServiceOptionsContent;

const {
  enable = true,
  eyebrow,
  title,
  description,
  items = [],
} = actualContent;

if (!enable || !items.length) {
  return null;
}
---

<section
  aria-labelledby="service-options-heading"
  class="section-spacing-vertical bg-theme-light overflow-hidden">
  <div class="container">
    <div class="grid items-end gap-8 lg:grid-cols-12 lg:gap-12">
      <div class="lg:col-span-7" data-scroll-reveal="blur-up">
        {
          eyebrow && (
            <p
              class="text-primary text-base-sm font-semibold tracking-widest uppercase md:text-base"
              set:html={markdownify(eyebrow)}
            />
          )
        }
        {
          title && (
            <h2
              id="service-options-heading"
              class="mt-3 max-w-3xl"
              set:html={markdownify(title)}
            />
          )
        }
      </div>

      {
        description && (
          <p
            class="text-text-default leading-body max-w-xl lg:col-span-5 lg:justify-self-end"
            data-scroll-reveal="fade-up"
            data-scroll-reveal-delay="100"
            set:html={markdownify(description)}
          />
        )
      }
    </div>

    <div
      class="border-border-light mt-10 border-b md:mt-12"
      data-scroll-reveal="fade-up"
      data-scroll-reveal-delay="160">
      {
        items.map((item, index) => (
          <article class="group border-border-light lg:hover:bg-primary/5 relative grid gap-5 border-t py-7 transition-colors duration-300 md:grid-cols-12 md:gap-6 lg:items-center lg:px-5 lg:py-8">
            <span class="text-primary/15 text-display-6 lg:text-display-5 leading-none font-semibold md:col-span-2 md:pt-1">
              {item.number || String(index + 1).padStart(2, "0")}
            </span>

            <div class="md:col-span-10 lg:col-span-7">
              <h3
                class="text-h5 md:text-h4-sm"
                set:html={markdownify(item.title)}
              />
              {item.tagline && (
                <p
                  class="text-dark mt-2 text-base font-semibold"
                  set:html={markdownify(item.tagline)}
                />
              )}
              <p
                class="text-text-default leading-body mt-3 max-w-2xl"
                set:html={markdownify(item.description)}
              />
            </div>

            <div class="border-border-light flex items-end justify-between gap-5 md:col-span-10 md:col-start-3 lg:col-span-3 lg:col-start-auto lg:border-s lg:ps-8">
              <div>
                <p class="text-primary text-sm font-semibold tracking-widest uppercase">
                  Best For
                </p>
                {item.bestFor && (
                  <p
                    class="text-dark mt-2 font-medium"
                    set:html={markdownify(item.bestFor)}
                  />
                )}
              </div>
              <Icons
                icon="ArrowRight"
                class="text-primary mb-1 size-5 shrink-0 transition-transform duration-300 lg:group-hover:translate-x-1"
                stroke-width="1.8"
                aria-hidden="true"
              />
            </div>
          </article>
        ))
      }
    </div>
  </div>
</section>
